# Installed to /etc/nginx/conf.d/wp-honeypot-limits.conf (conf.d is loaded
# inside the http{} block by Ubuntu's default nginx.conf).
limit_req_zone $binary_remote_addr zone=wp_honeypot_limit:10m rate=1r/s;
