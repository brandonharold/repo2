# Installed to /etc/fail2ban/filter.d/wp-honeypot-honeytoken.conf
# Matches the "honeypot" nginx log_format: <time> <ip> "<request>" ...
# No legitimate visitor ever requests a honeytoken path, so a single hit
# is enough to ban.
[Definition]
failregex = ^\S+\s+<HOST>\s+"
ignoreregex =
